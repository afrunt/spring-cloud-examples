server:
  port: 8000

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:8000/eureka

spring:
  application:
    name: EurekaServer

management:
  endpoints:
    web:
      exposure:
        include: "*"